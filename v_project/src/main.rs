fn main ( ) {

}
match match 
{
    Ok (_) => println!(" ")
}   Err (Error) => {
    println!("Error: {}", Error );
}
//}
//}
fn start_client (socket_address: String) -> IOResult<()> {

//client loop
loop { 
    // Read user input.
    let outgoing = {
        let 
        let
    }
}

//Tx user input to writer 
let _ = 

//Rx response from reader.
let incoming = {
    let 
    let


} ; 

let display_msg = String::from_utf8_lossy(&);
let display_msg = display_msg.trim();
let reset = SgrCode::Reset.to_string();
let display_msg = format!("{}", display_msg, reset);
println!("{}", display_msg);

//print debug
log::info!(); 

}






// new features : peer discovery, message passing between peers, err(e)